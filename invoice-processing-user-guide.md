# WorkBuddy 电子发票处理 Skill: 让报销整理变简单

每到整理报销的时候，发票总是件麻烦事。常见的流程是这样的：

1. 把每位员工的电子发票收上来，放进一个文件夹，比如「6 月电子发票汇总」
2. 逐张查看，按用途分类，比如交通费、差旅费
3. 算出每位员工每一类花了多少钱，再加出合计
4. 给每位员工填一份 `报销申请单.docx`，写清各类金额和合计
5. 把当月所有人的发票整理成一份 `月度汇总.xlsx` 留存

发票少的时候还能应付，员工一多，这些重复劳动既花时间也容易出错，算错金额、漏掉发票都是常有的事。

现在，这些工作可以交给 WorkBuddy。安装「电子发票处理」Skill 后，识别、分类、算金额、生成报销单和月度汇总，都由它自动完成。

## 效果预览

处理前

<img width="1784" height="1196" alt="img_v3_02150_804500e8-965a-4d5f-84b0-16a660cda6dg" src="https://github.com/user-attachments/assets/56727f19-919b-41ea-a5af-8b28fe19890a" />

处理后

<img width="1786" height="1198" alt="img_v3_02150_2456137f-df2f-4ef6-994a-c5cbdeb120eg" src="https://github.com/user-attachments/assets/919974ce-f6f5-4175-a037-959504ed0d29" />

## 使用方法

### 第一步：安装 Skill

先下载本 Skill 的安装包，然后在 WorkBuddy 中安装：

1. 打开 WorkBuddy，点击左侧导航栏的「专家\*技能\*连接器」
2. 在页面顶部切换到「技能」标签页
3. 点击右上角的「添加技能」按钮，在下拉菜单中选择「上传技能」
4. 在弹出的窗口中，把刚下载的安装包拖进去

安装完成后，点击「我安装的」，列表里出现 invoice-processing 就说明装好了。

首次使用时，如果电脑还没有所需的运行环境（Python），Skill 会先询问，确认后引导安装，跟着提示操作即可。

### 第二步：整理发票

把收到的发票按下面的样子放进文件夹：月份建一个总文件夹，每位员工一个子文件夹，发票文件直接放进去。

```
6 月电子发票汇总
├── 张三
│   ├── 电子发票1.pdf
│   ├── 电子发票2.png
│   └── 电子发票3.pdf
└── 李四
    ├── 电子发票1.png
    └── 电子发票2.pdf
......
```

两个要点：

- 子文件夹的名字就是报销单上的员工姓名，请使用真实姓名
- 发票属于哪一类由内容决定，和文件名没有关系，所以文件不需要改名

PDF、PNG 等常见格式都可以。

### 第三步：开始处理

1. 在 WorkBuddy 的「新建任务」输入框中输入「/」，会显示所有已安装的 Skill
2. 选择 invoice-processing
3. 把整理好的发票文件夹拖进输入框
4. 点击发送

剩下的就不用管了。处理过程中如果遇到拿不准的地方，比如发票上大小写金额对不上、图片模糊认不清，WorkBuddy 会停下来问你，确认后再继续，不会自己猜。

## 处理完成后会得到什么

所有文件都直接生成在发票文件夹里：

- **报销申请单（Word 文档）**：每位员工一份，各类金额、合计金额都填好，拿来就能用
- **月度汇总表（Excel 表格）**：当月所有人的发票记录，按人员和类别分好组、算好合计
- 发票原件也已按「人名 / 类别」自动归档，方便以后查找

单据里的发票默认先按日期从早到晚排，再按金额从小到大，再按编号从小到大。

## 定制化

默认设置不符合习惯，可以随时改。输入「/」调用 invoice-processing，把想改的地方直接告诉 WorkBuddy 就行。

发票默认分为四类：交通费、差旅费、餐饮费、办公费。想改名、想加类别，直接说：

* /invoice-processing 修改该 Skill，将办公费修改为日常费用
* /invoice-processing 修改该 Skill，添加新的电子发票类型，名称为「订阅服务」

生成的文件也能调整：

* /invoice-processing 修改该 Skill，生成的 `报销申请单.docx` 中，不需要每张电子发票的说明
* /invoice-processing 修改该 Skill，生成的 `月度汇总.xlsx` 中，不需要再进行分类

排序方式同样可以改：

* /invoice-processing 修改该 Skill，生成的 `报销申请单.docx` 中，电子发票按照发票日期「降序」排列
* /invoice-processing 修改该 Skill，生成的 `月度汇总.xlsx` 中，电子发票按照发票金额「降序」排列

## 几点提醒

- 员工文件夹的名称会出现在报销单上，请使用真实姓名
- 发票图片尽量清晰，识别会更准
- 金额一律以发票上的价税合计为准；大小写对不上时，WorkBuddy 会先询问再继续，不会自行修改
- 识别发票内容需要联网
- 处理结束后，留意输出的核对信息和问题清单，确认没有遗漏的发票，再提交报销
