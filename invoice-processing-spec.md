# WorkBuddy 电子发票处理 Skill 使用说明

## 概述

电子发票处理（invoice-processing）是面向 WorkBuddy 的发票整理 Skill。将按规范整理好的电子发票目录发送给 WorkBuddy，即可自动完成发票识别、分类与排序，并生成报销申请单（`*.docx`）与月度汇总表（`*.xlsx`），替代人工逐张整理的重复性工作。

## 背景说明

电子发票的报销处理通常包含以下环节：

1. 汇总各员工的电子发票，统一存放于指定文件夹，例如「6 月电子发票汇总」
2. 依据发票内容进行分类，例如交通费、差旅费等
3. 统计每位员工各分类的累计金额及合计金额
4. 为每位员工制作 `报销申请单.docx`，填写各分类金额与合计金额
5. 留存当月全部员工的发票记录，形成 `月度汇总.xlsx`

上述环节重复性强、耗时较长。在员工数量较多时，人工处理不仅效率有限，还容易出现遗漏或计算错误。安装本 Skill 后，上述工作可交由 WorkBuddy 自动完成。

## 功能特点

- 智能识别与分类：自动识别发票编号、日期、购买方名称与总金额，并归入对应费用类别
- 批量处理：支持多员工、多文件一次性处理
- 自动汇总：自动计算各分类金额与合计金额，并生成分组明细
- 存疑即停：遇到金额大小写不一致、内容模糊、分类不明确等情况时，暂停并等待确认，不自行推断
- 格式兼容：支持 PDF、PNG 等常见电子发票格式
- 灵活定制：支持自定义分类、输出内容与排序方式

## 效果预览

处理前

<img width="1784" height="1196" alt="img_v3_02150_804500e8-965a-4d5f-84b0-16a660cda6dg" src="https://github.com/user-attachments/assets/56727f19-919b-41ea-a5af-8b28fe19890a" />

处理后

<img width="1786" height="1198" alt="img_v3_02150_2456137f-df2f-4ef6-994a-c5cbdeb120eg" src="https://github.com/user-attachments/assets/919974ce-f6f5-4175-a037-959504ed0d29" />

## 使用方法

### 环境要求

本 Skill 依赖 Python 3.9 或更高版本，以及 python-docx、openpyxl 两个依赖包。首次运行时，Skill 会自动检测当前环境并引导完成安装，通常无需手动处理。

### 安装 Skill

**前提条件**：已下载本 Skill 的安装包。

**操作步骤**：

1. 打开 WorkBuddy，点击左侧导航栏的「专家·技能·连接器」入口
2. 在页面顶部切换至「技能」标签页
3. 点击右上角「添加技能」按钮，在下拉菜单中选择「上传技能」
4. 在弹出的窗口中，将已下载的 Skill 安装包拖入上传区域

**验证方式**：切换至「我安装的」视图，若列表中出现 invoice-processing，即表示安装成功。

### 发票收集

将各员工的电子发票按以下目录结构整理：

```
6 月电子发票汇总
├── 张三
│   ├── 电子发票1.pdf
│   ├── 电子发票2.png
│   └── 电子发票3.pdf
└── 李四
    ├── 电子发票1.png
    └── 电子发票2.pdf
......
```

**目录规范**：

- 顶层文件夹名称可自定义，建议包含年月信息，便于归档管理
- 二级文件夹以员工姓名命名，每位员工对应一个文件夹；人员归属以文件夹名称为准，Skill 不读取发票上的姓名信息
- 发票文件支持 PDF、PNG 等常见格式
- 发票分类仅依据发票内容识别，与文件名无关

### 调用 Skill

1. 在 WorkBuddy「新建任务」页面的输入框中输入「/」，显示全部已安装的 Skill
2. 选择 invoice-processing
3. 将整理好的电子发票汇总目录拖入输入框
4. 点击发送，WorkBuddy 将自动执行处理流程

## 处理流程

Skill 按以下顺序执行：识别信息 → 分类入文件夹 → 排序 → 生成报销申请单 → 月度汇总 → 交叉核对。处理过程中遇有存疑事项会暂停询问；处理结束后输出核对结果及问题清单。

### 分类规则

| 类别   | 适用范围                                       |
| ------ | ---------------------------------------------- |
| 餐饮费 | 餐费                                           |
| 差旅费 | 机票、行程单、高铁票、火车票、住宿费、退票费   |
| 交通费 | 打车费                                         |
| 办公费 | 饮料、办公用品、名片/logo/标书制作费等         |

除上述三类外的一切发票均归入办公费。每张发票仅归入一个类别；分类完成后，发票原件按「人员 / 类别」移动归档（移动而非复制，不保留副本）。

### 排序规则

生成的 `报销申请单.docx` 与 `月度汇总.xlsx` 中，每类发票按以下逻辑排序：

1. 发票日期按「升序」排列（从旧到新）
2. 发票金额按「升序」排列（从小到大）
3. 发票编号按「升序」排列（从小号到大号）

### 输出文件说明

所有生成文件均直接保存在源发票目录下。

**报销申请单（`*.docx`）**：按「员工 + 购买方」生成，每位员工针对每家购买方各一份。文件名为「姓名 + 报销申请单 + 购买方名称 + 生成日期」，例如 `张三报销申请单XX公司2026-8-14.docx`。单内按类别列示每张发票的日期、金额与说明，并给出各分类合计及总金额。

**月度汇总表（`*.xlsx`）**：汇总当月全部员工的发票记录，按人员、类别分组统计，并计算各组合计及当月总计。

### 存疑处理

遇有以下情形时，Skill 会暂停处理并等待确认，不会自行推断：

- 发票金额大写与小写不一致
- 发票内容模糊，无法完整识别
- 发票分类归属不明确

处理结束后，Skill 会输出本次未能处理的发票清单及原因，便于逐条核查。

## 定制化

Skill 默认将电子发票分为交通费、差旅费、餐饮费、办公费四类，各类适用范围见上文「分类规则」。如需调整分类、输出内容或排序方式，可通过「/」调用该 Skill，并向 WorkBuddy 发送修改要求。

**调整分类**：

- /invoice-processing 修改该 Skill，将办公费修改为日常费用
- /invoice-processing 修改该 Skill，添加新的电子发票类型，名称为「订阅服务」

**调整输出内容**：

- /invoice-processing 修改该 Skill，生成的 `报销申请单.docx` 中不需要每张电子发票的说明
- /invoice-processing 修改该 Skill，生成的 `月度汇总.xlsx` 中不需要再进行分类

**调整排序方式**：

- /invoice-processing 修改该 Skill，生成的 `报销申请单.docx` 中电子发票按发票日期「降序」排列
- /invoice-processing 修改该 Skill，生成的 `月度汇总.xlsx` 中电子发票按发票金额「降序」排列

## 注意事项

1. 员工文件夹名称将作为员工姓名写入输出文档，请使用真实姓名
2. 请尽量保证发票内容清晰可读，以提升识别与分类的准确率
3. 金额一律以发票上的价税合计为准，大小写不一致时系统会暂停并等待确认
4. 处理过程中需要网络连接以完成发票内容识别
5. 处理完成后，请留意 Skill 输出的核对信息与问题清单
